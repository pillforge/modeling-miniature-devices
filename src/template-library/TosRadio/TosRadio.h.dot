#ifndef {{=it.name.toUpperCase()}}_H
#define {{=it.name.toUpperCase()}}_H

typedef nx_struct {{=it.name}}DataMsg {
  {{=it.data_type}} data;
} {{=it.name}}DataMsg;

enum {
  AM_{{=it.name.toUpperCase()}} = 6
};

#endif
