#ifndef {{=it.name.toUpperCase()}}_H
#define {{=it.name.toUpperCase()}}_H
{{~it.includes :v}}
#include "{{=v}}"{{~}}

typedef nx_struct {{=it.name}}_t {{{~it.data_fields :v}}
  {{=v.type}} {{=v.name}}_data;{{~}}
} {{=it.name}}_t;

#endif
